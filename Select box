.select__container {
  position: relative;
  .select__button {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    padding: 15px;
    border: none;
    border-radius: 5px;
    background-color: $white;
    cursor: pointer;
    &--text{
        font-size: 15px;
    }
    &--icon{
        width: 13px;

    }
    &:hover {
      background-color: $medium-gray-alt;
    }
    &:active {
      background-color: $medium-gray;
    }
  }
  .select__options {
    border-radius: 4px;
    background-color: $white;
    width: 100%;
    padding: 1px;
    margin-top: 2px;
    position: absolute;
    .options__list {
      margin: 0;
      padding: 10px 0;
      list-style: none;
      &--item {
        //margin-bottom: 10px;
        padding: 5px 0 8px 15px;
        width: 100%;
        cursor: pointer;
        &:hover {
          background-color: $light-gray;
          color: $rose;
        }
      }
    }
  }
}
.select{
  width: 100%;
  height: 45px;
  border-radius: 4px;
  padding: 0 20px;
  border: none;
  background-color: $white;
  display: flex;
  align-items: center;
  justify-content: space-between;
  &__value{
    font-size: 15px;
    margin-right: 5px;
  }
  &:hover{
    background-color: $light-gray-alt;
  }
  &:active{
    background-color: $medium-gray;
  }
  .arrow{
    width: 12px;
    filter: invert(0.4) sepia(0.5) saturate(0.5) hue-rotate(50deg);
  }
}
